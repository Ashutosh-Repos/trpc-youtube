{
    "name": "youtube",
    "version": "1.0.0",
    "description": "Youtube like video streaming platform project",
    "main": "index.js",
    "scripts": {
        "dev": "pnpm --recursive --parallel dev",
        "build": "pnpm --recursive build",
        "start": "pnpm --recursive --parallel start",
        "lint": "pnpm --recursive lint",
        "sync:schema": "cp apps/server/prisma/schema.prisma apps/client/prisma/schema.prisma && pnpm --filter @youtube/client exec prisma generate",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "private": true,
    "license": "ISC",
    "packageManager": "pnpm@10.4.1"
}
